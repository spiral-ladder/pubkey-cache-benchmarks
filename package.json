{
  "name": "lodestar-pubkey-cache-benchmarks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "bench": "NODE_OPTIONS='--import tsx' benchmark --config .benchrc.yaml src/benchmark.ts",
    "check": "pnpm typecheck && pnpm test",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@chainsafe/benchmark": "^2.0.2",
    "@chainsafe/lodestar-z": "file:../lodestar-z",
    "@chainsafe/pubkey-index-map": "3.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  }
}
